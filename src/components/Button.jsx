import React from "react";
import { Link } from "react-router-dom";

const Button = () => {
  return (
    <a href="/contact" className="c-button is-white u-font-serif">
      Get in touch!
    </a>
  );
};

export default Button;
